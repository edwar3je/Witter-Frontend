import React, { useState, useEffect } from 'react';
import { Link } from 'react-router-dom';
import './UserCard.css';

const UserCard = ({ userInfo }) => {
    return (
        <h1>You have reached the user card component</h1>
    )
}

export default UserCard;